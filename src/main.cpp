#include "../include/Grafo.hpp"
#include "../include/Escalonador.hpp"
#include <iostream>

int main() {

    //leio as informações do grafo de armazens 
    /*
    int quantidadeArmazens;
    std::cin >> quantidadeArmazens;
    Grafo grafo(quantidadeArmazens);

    for(int i = 0; i < quantidadeArmazens; i++) {

    }
    */

    return 0;
}